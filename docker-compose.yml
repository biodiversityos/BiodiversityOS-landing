services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
    ports:
      - "127.0.0.1:${PORT:-3002}:3000"
    env_file:
      - .env
    restart: unless-stopped
